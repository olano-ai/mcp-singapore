# Olano Singapore

The complete `@olano/mcp-singapore` server with all eight focused Agent Skills. It exposes the
`all` tool profile and starts automatically when this Claude Code plugin is enabled.

Optional keys unlock additional production and live-data sources. The plugin stores configured
values as sensitive user configuration; never commit keys to the repository.
