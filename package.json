{
  "name": "@olano/singapore-mcp-monorepo",
  "version": "0.4.1",
  "private": true,
  "description": "Open-source MCP servers for Singapore public data and services",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @olano/mcp-core && npm run build -w @olano/mcp-datagov && npm run build -w @olano/mcp-onemap && npm run build -w @olano/mcp-lta && npm run build -w @olano/mcp-weather && npm run build -w @olano/mcp-catalog && npm run build -w @olano/mcp-finance-sg && npm run build -w @olano/mcp-analytics && npm run build -w @olano/mcp-insights-sg && npm run build -w @olano/mcp-rail-sg && npm run build -w @olano/mcp-singapore && npm run build -w @olano/sg-cli",
    "clean": "npm run clean --workspaces --if-present",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "eslint .",
    "check:plugins": "node scripts/check-claude-plugins.mjs && node scripts/check-openai-plugins.mjs",
    "build:brand": "node scripts/build-brand.mjs",
    "check:brand": "node scripts/build-brand.mjs --check",
    "check:workflows": "node scripts/check-workflow-security.mjs",
    "build:mcpb": "node scripts/build-mcpb.mjs",
    "check:mcpb": "node scripts/check-mcpb.mjs",
    "test": "npm run build && vitest run && node scripts/check-capability-coverage.mjs && npm run check:plugins",
    "test:watch": "vitest"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@eslint/js": "^9.34.0",
    "@modelcontextprotocol/client": "^2.0.0",
    "@types/node": "^24.3.0",
    "eslint": "^9.34.0",
    "prettier": "^3.6.2",
    "typescript": "^5.9.2",
    "typescript-eslint": "^8.40.0",
    "vitest": "^3.2.4"
  }
}
