{
  "name": "@olano/singapore-mcp-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "Open-source MCP servers for Singapore public data and services",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @olano/mcp-core && npm run build -w @olano/mcp-datagov && npm run build -w @olano/mcp-onemap && npm run build -w @olano/mcp-lta && npm run build -w @olano/mcp-weather && npm run build -w @olano/mcp-singapore",
    "clean": "npm run clean --workspaces --if-present",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "eslint .",
    "test": "npm run build && vitest run",
    "test:watch": "vitest"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@eslint/js": "^9.34.0",
    "@modelcontextprotocol/client": "^2.0.0",
    "@types/node": "^24.3.0",
    "eslint": "^9.34.0",
    "prettier": "^3.6.2",
    "typescript": "^5.9.2",
    "typescript-eslint": "^8.40.0",
    "vitest": "^3.2.4"
  }
}
