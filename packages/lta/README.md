# @olano/mcp-lta

Read-only MCP tools for live Singapore transport data from LTA DataMall.

```bash
LTA_DATAMALL_API_KEY=your-account-key npx @olano/mcp-lta
```

All dynamic DataMall APIs require an Account Key issued to a registered subscriber. The key is sent
only in the upstream `AccountKey` header.
