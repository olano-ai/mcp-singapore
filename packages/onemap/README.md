# @olano/mcp-onemap

Read-only address search, reverse-geocoding, and routing tools for Singapore OneMap.

```bash
npx @olano/mcp-onemap
```

Set `ONEMAP_TOKEN` to an access token generated through your OneMap API account. OneMap currently
requires the token for these API operations.
