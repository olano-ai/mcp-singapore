# @olano/mcp-weather

Friendly, read-only MCP tools for Singapore weather and PSI feeds from NEA/MSS via data.gov.sg.

```bash
npx @olano/mcp-weather
```

`DATA_GOV_SG_API_KEY` is optional for testing and recommended for higher production limits.
